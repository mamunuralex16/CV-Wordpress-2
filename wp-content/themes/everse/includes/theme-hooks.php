<?php
/**
 * Theme hooks.
 * @author  	 DeoThemes
 * @copyright  (c) Copyright by DeoThemes
 * @link       https://deothemes.com
 * @package 	 Everse
 * @since 		 1.0.0
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit( 'Direct script access denied.' );
}

/**
 * Head top
 */
function everse_head_top() {
	do_action( 'everse_head_top' );
}

/**
 * Head bottom
 */
function everse_head_bottom() {
	do_action( 'everse_head_bottom' );
}

/**
 * Header before
 */
function everse_header_before() {
	do_action( 'everse_header_before' );
}

/**
 * Site Header
 */
function everse_header() {
	do_action( 'everse_header' );
}

/**
 * Header after
 */
function everse_header_after() {
	do_action( 'everse_header_after' );
}

/**
 * Masthead
 */
function everse_masthead() {
	do_action( 'everse_masthead' );
}

/**
 * Menu before
 */
function everse_menu_before() {
	do_action( 'everse_menu_before' );
}

/**
 * Menu after
 */
function everse_menu_after() {
	do_action( 'everse_menu_after' );
}

/**
 * Last menu item after
 */
function everse_last_menu_item_after() {
	do_action( 'everse_last_menu_item_after' );
}

/**
 * Body top
 */
function everse_body_top() {
	do_action( 'everse_body_top' );
}

/**
 * Body bottom
 */
function everse_body_bottom() {
	do_action( 'everse_body_bottom' );
}

/**
 * Primary content top
 */
function everse_primary_content_top() {
	do_action( 'everse_primary_content_top' );
}

/**
 * Primary content before
 */
function everse_primary_content_before() {
	do_action( 'everse_primary_content_before' );
}

/**
 * Primary content query
 */
function everse_primary_content_query() {
	do_action( 'everse_primary_content_query' );
}

/**
 * Primary content after
 */
function everse_primary_content_after() {
	do_action( 'everse_primary_content_after' );
}

/**
 * Primary content bottom
 */
function everse_primary_content_bottom() {
	do_action( 'everse_primary_content_bottom' );
}

/**
 * Comments before
 */
function everse_comments_before() {
	do_action( 'everse_comments_before' );
}


/**
 * Comments
 */
function everse_comments() {
	do_action( 'everse_comments' );
}

/**
 * Comments after
 */
function everse_comments_after() {
	do_action( 'everse_comments_after' );
}


/**
 * Entry section before
 */
function everse_entry_section_before() {
	do_action( 'everse_entry_section_before' );
}

/**
 * Entry header before
 */
function everse_entry_header_before() {
	do_action( 'everse_entry_header_before' );
}

/**
 * Entry header
 */
function everse_entry_header() {
	do_action( 'everse_entry_header' );
}

/**
 * Entry header after
 */
function everse_entry_header_after() {
	do_action( 'everse_entry_header_after' );
}

/**
 * Content before
 */
function everse_content_before() {
	do_action( 'everse_content_before' );
}

/**
 * Content top
 */
function everse_content_top() {
	do_action( 'everse_content_top' );
}

/**
 * Content bottom
 */
function everse_content_bottom() {
	do_action( 'everse_content_bottom' );
}

/**
 * Content after
 */
function everse_content_after() {
	do_action( 'everse_content_after' );
}


/**
 * Entry featured image
 */
function everse_entry_featured_image() {
	do_action( 'everse_entry_featured_image' );
}

/**
 * Entry content top
 */
function everse_entry_content_top() {
	do_action( 'everse_entry_content_top' );
}

/**
 * Entry content bottom
 */
function everse_entry_content_bottom() {
	do_action( 'everse_entry_content_bottom' );
}

/**
 * Page title before
 */
function everse_page_title_before() {
	do_action( 'everse_page_title_before' );
}

/**
 * Page title after
 */
function everse_page_title_after() {
	do_action( 'everse_page_title_after' );
}


/**
 * Sidebar before
 */
function everse_sidebar_before() {
	do_action( 'everse_sidebar_before' );
}

/**
 * Sidebar after
 */
function everse_sidebar_after() {
	do_action( 'everse_sidebar_after' );
}

/**
 * Footer before
 */
function everse_footer_before() {
	do_action( 'everse_footer_before' );
}

/**
 * Footer
 */
function everse_footer() {
	do_action( 'everse_footer' );
}

/**
 * Footer after
 */
function everse_footer_after() {
	do_action( 'everse_footer_after' );
}